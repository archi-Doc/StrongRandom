<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="StrongRandom.Presentation.PresentationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StrongRandom.Presentation"
    xmlns:Arc="using:Arc.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Arc:Stringer Source=Presentation.Enter}" Margin="10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="textBox1" PlaceholderText="Number" Width="120" Margin="10" />
            <Button Content="{Arc:Stringer Source=Presentation.Button}" Click="Button_Click" Margin="10" />
            <TextBox x:Name="textBox2" IsReadOnly="True" Width="120" Margin="10" />
        </StackPanel>
    </Grid>
</Page>
